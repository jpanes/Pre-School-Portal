json.extract! subject, :id, :subject_name, :first_grading, :second_grading, :third_grading, :fourth_grading, :Final_grade, :student_id, :created_at, :updated_at
json.url subject_url(subject, format: :json)
